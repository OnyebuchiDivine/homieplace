import 'package:flutter/material.dart';

class Paulpage extends StatelessWidget {
  const Paulpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Image.asset(""),
    );
  }
}
